"use client";

import Event25TeamsTable from "./Event25TeamsTable";

export default function Event26TeamsTable({ teams }: { teams: any[] }) {
  return <Event25TeamsTable teams={teams} gameYear={2026} />;
}
